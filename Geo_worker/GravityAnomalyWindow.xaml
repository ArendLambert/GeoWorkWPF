<Window x:Class="Geo_worker.GravityAnomalyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="График аномалии гравитации" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding PicketName}" FontWeight="Bold" Margin="0,0,0,10"/>

        <oxy:PlotView Grid.Row="1" Model="{Binding PlotModel}" Margin="0,0,0,10"/>

        <Button Grid.Row="2" Content="Закрыть" Width="80" HorizontalAlignment="Right" Click="Close_Click"/>
    </Grid>
</Window>